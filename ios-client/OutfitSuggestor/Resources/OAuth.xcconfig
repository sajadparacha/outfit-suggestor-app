// OAuth client IDs for Google Sign-In (leave blank until Cloud Console values are set).
// iOS client ID from Google Cloud → Credentials → iOS OAuth client
GOOGLE_IOS_CLIENT_ID =
// Web client ID (used as audience for backend token verification / GIDServerClientID)
GOOGLE_SERVER_CLIENT_ID =
// Reversed iOS client ID (shown as "iOS URL scheme" in Cloud Console)
GOOGLE_REVERSED_CLIENT_ID =
